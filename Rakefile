require_relative './config/environment'
require 'sinatra/activerecord/rake' #The commands listed when running rake -T are made available as Rake tasks through here 

task :console do
  require 'irb'
  ARGV.clear
  IRB.start
end
